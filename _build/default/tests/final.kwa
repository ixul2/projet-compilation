c el;

class c{
  final int x;
  int y;

  void constructor(int x, int y) {
    this.x = x;
    this.y = y;
  }

  void change_x(){ 
    //this.x = 5; //program fails
  }
}

main {
  el = new c(1, 2);
  print(el.x);
}